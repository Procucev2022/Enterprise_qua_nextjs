# Kiro Unit Testing Rules
- Every file in `app/` and `lib/` must have >= 90% unit test code coverage on Statements, Branches, Functions, Lines.
- Global timeout: 20000ms.
- Run `npm run test:coverage` to verify.
